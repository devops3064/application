
git init = this command is used to create a initial empty git repo
git config --global user.name "ravi reddy"
git config --global user.email "srcreddy3064@gmail.com"
git config --global --list
git add .
git commit -m "initial commit"
git log
git wrong command to test going back to previous commit
git rm --cached filename = to unstage a file from staging area
git branch newbranchname = it creates a new branch
git merge branchname(Feature1) = this command will merge the branch(Feature1) to the main branch